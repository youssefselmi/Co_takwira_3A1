<form method="post" action="{{ path('entrainement_delete', {'idEntrainement': entrainement.idEntrainement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrainement.idEntrainement) }}">
    <button class="btn">Delete</button>
</form>
