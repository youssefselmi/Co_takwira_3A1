<form method="post" action="{{ path('commentaire_delete', {'id': commentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.id) }}">
    <button style="color:purple;border: 0.001px;background-color: transparent;cursor: pointer; "><i class="fa fa-trash" aria-hidden="true" fa-3x></i></button>
</form>